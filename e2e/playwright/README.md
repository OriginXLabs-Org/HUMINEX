# Playwright E2E

Primary automation lives in `Huminex-Automation/playwright`.

This folder is kept to align with target repository layout:

- `e2e/playwright` (layout-compatible)
- `Huminex-Automation` (active automation suite)
