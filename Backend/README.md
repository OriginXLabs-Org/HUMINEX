# Backend Workspace (Target Layout)

This folder mirrors the target enterprise structure.

Current active backend implementation remains in `Backend/`.
Use this path during planned migration to lowercase layout.
