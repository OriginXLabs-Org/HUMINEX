name: OpenHuman Image Push
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: Build/push OpenHuman image to ACR"
