name: OpenHuman E2E
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: OpenHuman E2E Playwright"
